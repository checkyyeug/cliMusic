# cliMusic Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-07

## Active Technologies

- C++17 (core CLI modules), Python 3.11+ (daemon scripts, testing) + FFmpeg (audio decoding), PortAudio (audio output), libsamplerate (resampling), FFTW3 (FFT computation), nlohmann/json (JSON), spdlog (logging) (001-xpu-mvp-system)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

C++17 (core CLI modules), Python 3.11+ (daemon scripts, testing): Follow standard conventions

## Recent Changes

- 001-xpu-mvp-system: Added C++17 (core CLI modules), Python 3.11+ (daemon scripts, testing) + FFmpeg (audio decoding), PortAudio (audio output), libsamplerate (resampling), FFTW3 (FFT computation), nlohmann/json (JSON), spdlog (logging)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
